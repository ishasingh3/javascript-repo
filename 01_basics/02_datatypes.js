"use strict"; //treats all js code as newer version

// alert("hello") //will give error as not using on browser (using nodejs)

console.log(3 + 3); //make sure code is readable

// different datatypes

// number (upto 2^53)
let age = 19

// "bigint" to store big numbers

// string (can use "" or '')
let name = "isha"

//boolean (either true or false)
let access = "false"

//null
let salary = null //js can take variable value as null but the 'type' will show oject

//undefined 
let price;

//symbol (unique)

console.log(typeof salary); //output = 'object'
console.log(typeof price);